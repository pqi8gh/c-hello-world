version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: build
          command: ./build.sh

workflows:
  build_and_release:
    jobs:
      - build
